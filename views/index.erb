<ul>
    <% @memos.each do |id,memo| %>
        <li>
            <a href="/memos/<%= id %>"><%= h memo.title %></a>
        </li>
    <% end %>
</ul>
<button type=“button” onclick="location.href='/memos/new'">追加</button>
